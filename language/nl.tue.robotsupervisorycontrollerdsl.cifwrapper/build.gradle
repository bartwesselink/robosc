/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java library project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.3.1/userguide/building_java_projects.html
 */

plugins {
    // Apply the java-library plugin for API and implementation separation.
    id 'java-library'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
	api fileTree(dir: './escet-repo/common', includes: ['**/*.jar'])
	api fileTree(dir: './escet-repo/cif', includes: ['**/*.jar'])
	api fileTree(dir: './escet-repo/thirdparty', includes: ['**/*.jar'])
	api fileTree(dir: './escet-repo/setext', includes: ['**/*.jar'])
	api 'org.apache.commons:commons-lang3:3.12.0'
	api 'commons-lang:commons-lang:2.4'
	api 'commons-io:commons-io:2.6'
	api 'org.eclipse.core:org.eclipse.core.resources:3.7.100'
    api "${swtGroupId}:${swtArtifact}:${swtVersion}"
}

jar {
    duplicatesStrategy = 'exclude'
    from (configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }) {
        exclude 'META-INF/MANIFEST.MF'
        exclude 'META-INF/*.SF'
        exclude 'META-INF/*.DSA'
        exclude 'META-INF/*.RSA'
    }
}

ext.repository = 'https://gitlab.eclipse.org/bartwesselink/escet.git';
ext.branch = 'feature/add-uncontrollable-events-and-individual-event-execution';

task downloadAndBuildEscet(type: Exec) {
    commandLine './build_escet.sh', repository, branch

    inputs.property("is-built", true)
    outputs.dir "escet-repo"
}

build.dependsOn downloadAndBuildEscet
jar.dependsOn downloadAndBuildEscet